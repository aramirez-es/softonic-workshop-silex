{% extends 'layout.twig' %}

{% block content %}
<h1>Welcome to OpenTonic</h1>
    <ul>
    {% for worklog in worklogs %}
        <li><strong>{{ worklog.name }}</strong> says</li>
        <div>{{ worklog.text }}</div>
    {% endfor %}
    </ul>
{% endblock %}
